@model IEnumerable<LocacaoVeiculos.Models.Combustivel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.combustivel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.combustivel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CombustivelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CombustivelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CombustivelID })
        </td>
    </tr>
}

</table>
